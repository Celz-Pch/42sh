name: Security Audit

on:
  push:
    paths: 
      - '.release-manifest.json'
  pull_request:
    paths: 
      - '.release-manifest.json'
  schedule:
    - cron: '0 0 * * 1'

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run security-audit
        run: echo "unsuported in C"
